name: modulus
channels:
    - conda-forge
    - nvidia
dependencies:
    - python=3.11
# Basics
    - numpy
    - matplotlib
# xarray et al
    - xarray
    - netCDF4
    - h5netcdf
    - bottleneck
    - dask[complete]
    - zarr
    - cftime
# jupyter etc
    - jupyter
    - ipython
    - ipykernel
# filesystem
    - fsspec
    - gcsfs
# graphcast
    - cartopy
    - cuda-nvcc
    - pandas
    - google-api-python-client
# WeatherBench2
    - apache-beam
    - xarray-beam
    - xesmf
    - pytorch
    - torchvision
    - xbatcher
# legit progress bar
    - tqdm
    - pip
    - pip:
      - nvidia-modulus[all]
